#ifndef MENU_H
#define MENU_H
int menu();
int menu_add(Table *board);
int menu_find(Table *board);
int menu_delete(Table *board);
int menu_cleanShow(Table *board);
int menu_dirtyShow(Table *board);
#endif
